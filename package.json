{
  "name": "project",
  "version": "1.0.0",
  "proxy": "http://localhost:5001",
  "scripts": {
    "start": "react-scripts start",
    "start:server": "node server/server.js",
    "build": "react-scripts build",
    "build:deploy": "npm run build && echo 'Run: node server/server.js (with NODE_ENV=production)'",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "react": "^18.2.0",
    "react-custom-roulette": "^1.4.1",
    "react-dom": "^18.2.0",
    "react-router-dom": "^7.1.2",
    "react-scripts": "^5.0.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
